{
  "name": "travelpal-ai",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "homepage": "./",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "cap:init": "npx cap init",
    "cap:sync": "npx cap sync",
    "android-ci-build": "npm run build && npx cap add android || true && npx cap sync android --deploy && (cd android && ./gradlew bundleRelease || true)"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "antd-mobile": "^5.41.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.19",
    "gh-pages": "^6.1.1",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.3",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
